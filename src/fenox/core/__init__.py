"""Fenox core: the engine shared by the hub and the CLI.

No web framework, no argparse, no terminal UI. Everything here is plain Python so
it can be driven by the server, the CLI, or tests without either side present.
"""
